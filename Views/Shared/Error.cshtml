@model ErrorViewModel
@{
    ViewData["Title"] = "Oops! Algo deu errado.";
}

<div class="container">
    <div class="row mt-5">
        <div class="col-12 text-center">
            <h1 class="display-4 text-danger"><i class="fas fa-exclamation-circle"></i> Oops! Algo deu errado.</h1>
            <h3 class="text-muted">@Model.Message</h3>
        </div>
    </div>

    @if (Model.ShowRequestId)
    {
        <div class="row mt-3">
            <div class="col-12 text-center">
                <p class="text-info">
                    <strong>ID da Solicitação:</strong> <code>@Model.RequestId</code>
                </p>
            </div>
        </div>
    }

    <div class="row mt-3">
        <div class="col-12 text-center">
            <h4>O que você pode fazer?</h4>
            <p class="text-muted">Se você é um usuário, tente o seguinte:</p>
            <ul class="text-muted">
                <li>Atualize a página ou tente novamente mais tarde.</li>
                <li>Entre em contato com o suporte se o problema persistir.</li>
            </ul>
            <a href="/" class="btn btn-primary mt-2"><i class="fas fa-home"></i> Voltar para a Home</a>
        </div>
    </div>

    <div class="row mt-5">
        <div class="col-12">
            <hr />
            <p class="text-center text-muted">
                <strong>Nota:</strong> Para ver informações detalhadas sobre o erro, defina a variável <code>ASPNETCORE_ENVIRONMENT</code> como <strong>Desenvolvimento</strong> nas configurações da sua aplicação.
            </p>
        </div>
    </div>
</div>
